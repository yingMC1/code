#include <bits/stdc++.h>

#define endl '\n'
using namespace std;
using i64 = long long;
const int N = 200000;

int n, q, s, t, a[N];

void update(int x, int l, int r, int q, int w) {
    if (l == r) {
    }
}
int main() {
    cin >> n >> q >> s >> t;
    for (int i = 1; i <= n; i++)
        cin >> a[i];
    for (int i = 1; i <= q; i++) {
    }
    return 0;
}